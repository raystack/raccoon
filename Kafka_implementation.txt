
KAFKA HANDLER

//
Have a publisher wrapping a kafka client
// 
create the kafka client, need to initialize with publisher configs
return this client
//
push messages which will take (key, value)
return error, how we will handle this
//
close the publisher when the server sends a shutdown signal

//
test kafka client which will do the following
 - send a message and verify that the api accepts and returns success

KAFKA CONFIG INIT
// config package has all config related functions
// load the config from the file, 1 config for all properties
// function to return the config properties

//Server Starts Up
- Config loads all the properties
- Start Kakfa client
- Need a way to send a context of whether the server is running or shuttting down and this needs to be  passed to the kafka publisher
